# grafana-util Python Package

This directory is the Python packaging root for `grafana-util`.

The full project overview, operator documentation, and repository guidance live in the
repository root `README.md` and `docs/`.
