# ai-changes.md

Current AI change log only.

- Older detailed history moved to [`archive/ai-changes-archive-2026-03-24.md`](docs/internal/archive/ai-changes-archive-2026-03-24.md).
- Detailed 2026-03-27 entries moved to [`archive/ai-changes-archive-2026-03-27.md`](docs/internal/archive/ai-changes-archive-2026-03-27.md).
- Detailed 2026-03-28 task notes were condensed into [`archive/ai-changes-archive-2026-03-28.md`](docs/internal/archive/ai-changes-archive-2026-03-28.md).
- Detailed 2026-03-29 through 2026-03-31 entries moved to [`archive/ai-changes-archive-2026-03-31.md`](docs/internal/archive/ai-changes-archive-2026-03-31.md).
- Detailed 2026-04-01 through 2026-04-12 entries moved to [`archive/ai-changes-archive-2026-04-12.md`](docs/internal/archive/ai-changes-archive-2026-04-12.md).
- Keep this file limited to the latest active architecture and maintenance changes.
- Older entries moved to [`ai-changes-archive-2026-04-13.md`](docs/internal/archive/ai-changes-archive-2026-04-13.md).
- Older entries moved to [`ai-changes-archive-2026-04-14.md`](docs/internal/archive/ai-changes-archive-2026-04-14.md).
- Older entries moved to [`ai-changes-archive-2026-04-15.md`](/Users/kendlee/work/grafana-utils/docs/internal/archive/ai-changes-archive-2026-04-15.md).

## 2026-04-15 - Fix stale dashboard command references
- Summary: corrected stale public command information by replacing README `dashboard analyze` examples with the canonical `dashboard summary` command, changing dashboard policy help/docs from inspect-artifact wording to summary JSON artifact wording, and clarifying maintainer docs that dashboard inspection is an internal artifact flow rather than a public `dashboard inspect` command.
- Tests: no behavior change beyond CLI help text; regenerated command manpages and HTML docs after the help/docs wording update.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet help_rust_tests`; `make man`; `make html`; `make man-check`; `make html-check`; `make quality-docs-surface`; `make quality-ai-workflow`; `git diff --check`; command smoke for rejected `dashboard analyze --help` and `dashboard inspect-export --help`, plus accepted `dashboard summary --help`.
- Impact: `README.md`, `README.zh-TW.md`, dashboard policy command docs/generated docs, `rust/src/cli/mod.rs`, maintainer docs, and AI trace docs. Python implementation was intentionally left unchanged.
- Rollback/Risk: low docs/help correction. Rollback would reintroduce removed command examples and stale inspect-artifact wording.
- Follow-up: keep `scripts/contracts/command-surface.json` and `scripts/check_ai_workflow.py` removed-command guardrails in place so `dashboard analyze` does not return to public docs.

## 2026-04-15 - Clear remaining Rust architecture warnings
- Summary: cleared the remaining Rust architecture warnings without changing public CLI behavior. Sync workspace help tests now use grouped semantic help assertions; dashboard dependency contract, datasource staged-reading, and dashboard browse-support regression tests moved into focused sibling test modules; the sync live-apply request-json shim moved into a test-only sibling module.
- Tests: preserved sync help, dashboard dependency contract, sync live apply, datasource project-status, and dashboard browse coverage while reducing large warning hotspots.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet cli_help_rust_tests`; `cargo test --manifest-path rust/Cargo.toml --quiet dependency_contract`; `cargo test --manifest-path rust/Cargo.toml --quiet sync_live`; `cargo test --manifest-path rust/Cargo.toml --quiet project_status`; `cargo test --manifest-path rust/Cargo.toml --quiet dashboard_browse`; `cargo test --manifest-path rust/Cargo.toml --quiet`; `cargo clippy --manifest-path rust/Cargo.toml --all-targets -- -D warnings`; `make quality-architecture`.
- Impact: `rust/src/commands/sync/cli_help_rust_tests.rs`, `rust/src/commands/dashboard/inspection/dependency_contract.rs`, `rust/src/commands/dashboard/inspection/dependency_contract_rust_tests.rs`, `rust/src/grafana/api/sync_live_apply.rs`, `rust/src/grafana/api/sync_live_apply_request.rs`, `rust/src/commands/datasource/project_status/staged_reading.rs`, `rust/src/commands/datasource/project_status/staged_reading_rust_tests.rs`, `rust/src/commands/dashboard/browse_support.rs`, `rust/src/commands/dashboard/browse_support_rust_tests.rs`, and AI trace docs. README files, Python implementation, and dashboard summary/analyze naming were intentionally left unchanged.
- Rollback/Risk: low to medium maintainability refactor. Most changes are test-only splits; the sync request-json helper is test-only and keeps the same public test entrypoint. Rollback would collapse sibling modules back into their parent files.
- Follow-up: handle stale public wording around `dashboard summary` versus removed `dashboard analyze` in a separate docs/contract pass.

## 2026-04-15 - Reduce dashboard help assertions
- Summary: replaced repeated direct `help.contains()` assertions in dashboard inspect/help regressions with a shared `assert_help_includes` helper for grouped semantic help checks. The expected dashboard help fragments are unchanged.
- Tests: preserved dashboard summary, validate-export, policy, dependencies, impact, variables, and full-help coverage while reducing the direct assertion pattern that architecture guardrails flag.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet dashboard_cli_inspect_help`; `cargo test --manifest-path rust/Cargo.toml --quiet`; `cargo clippy --manifest-path rust/Cargo.toml --all-targets -- -D warnings`; `make quality-architecture`; `git diff --check`.
- Impact: `rust/src/commands/dashboard/dashboard_cli_inspect_help_rust_tests.rs` and AI trace docs. README files and Python implementation were intentionally left unchanged.
- Rollback/Risk: low test-only refactor. Rollback would restore the prior direct `help.contains()` assertions.
- Follow-up: apply the same semantic-help assertion cleanup to `sync/cli_help_rust_tests.rs`.

## 2026-04-15 - Split datasource supported catalog tests
- Summary: split supported datasource catalog output tests out of `cli_mutation.rs` into `cli_mutation_supported_catalog.rs`. The parent module remains focused on command help, parser compatibility, and add-payload behavior.
- Tests: preserved existing supported catalog assertions for JSON fixture projection, profile metadata, family defaults, text, table, csv, and yaml output.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet datasource_`; `cargo test --manifest-path rust/Cargo.toml --quiet`; `cargo clippy --manifest-path rust/Cargo.toml --all-targets -- -D warnings`; `make quality-architecture`; `git diff --check`.
- Impact: `rust/src/commands/datasource/tests/cli_mutation.rs`, `rust/src/commands/datasource/tests/cli_mutation_supported_catalog.rs`, `rust/src/commands/datasource/tests/mod.rs`, and AI trace docs. README files and Python implementation were intentionally left unchanged.
- Rollback/Risk: low test-only split. Rollback would move supported catalog assertions back into the datasource CLI mutation test module.
- Follow-up: continue remaining architecture-warning hotspots: dashboard browse support, dashboard dependency contract, datasource staged reading, sync live apply, and help-test semantic assertions.

## 2026-04-15 - Split datasource tail import and inspect tests
- Summary: split datasource tail import validation/loader tests and inspect-export/local-source tests into focused sibling modules. The parent tail test module now keeps routed datasource import identity, summary, and export-org routing behavior.
- Tests: preserved existing datasource import loader, inspect-export renderer, manifest classifier, local-source help, and routed import assertions while moving them into responsibility-based modules.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet datasource_`; `cargo test --manifest-path rust/Cargo.toml --quiet`; `cargo clippy --manifest-path rust/Cargo.toml --all-targets -- -D warnings`; `make quality-architecture`; `git diff --check`.
- Impact: `rust/src/commands/datasource/tests/tail.rs`, `rust/src/commands/datasource/tests/tail_import.rs`, `rust/src/commands/datasource/tests/tail_inspect.rs`, and AI trace docs. README files and Python implementation were intentionally left unchanged.
- Rollback/Risk: low test-only split. Rollback would move import and inspect-export tests back into the parent datasource tail module.
- Follow-up: continue remaining architecture-warning hotspots: dashboard browse support, dashboard dependency contract, datasource staged reading, datasource CLI mutation tests, sync live apply, and help-test semantic assertions.

## 2026-04-15 - Split snapshot review tests
- Summary: split snapshot staged-scope resolver tests and snapshot review wrapper/warning tests out of `snapshot/tests.rs` into focused sibling modules. The parent test module keeps shared fixture builders and broader snapshot export/review coverage.
- Tests: preserved existing snapshot assertions while moving staged export scope resolver coverage and review warning/wrapper coverage to dedicated modules.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet snapshot`; `cargo test --manifest-path rust/Cargo.toml --quiet`; `cargo clippy --manifest-path rust/Cargo.toml --all-targets -- -D warnings`; `make quality-architecture`; `git diff --check`.
- Impact: `rust/src/commands/snapshot/tests.rs`, `rust/src/commands/snapshot/tests_staged_scopes.rs`, `rust/src/commands/snapshot/tests_review_warnings.rs`, and AI trace docs. README files and Python implementation were intentionally left unchanged.
- Rollback/Risk: low test-only split. Rollback would move the staged-scope and review warning tests back into the parent snapshot test module.
- Follow-up: continue remaining architecture-warning hotspots: dashboard browse support, dashboard dependency contract, datasource staged reading, datasource CLI mutation/tail tests, sync live apply, and help-test semantic assertions.

## 2026-04-15 - Split access runtime user tests
- Summary: split user-focused runtime coverage out of `access_runtime_org_rust_tests.rs` into `access_runtime_user_rust_tests.rs`. The new module owns user diff routing, user diff count behavior, global user export/import/diff coverage, org user export/diff-with-teams coverage, and local user list input-dir routing.
- Tests: preserved existing access runtime assertions and moved tests without changing behavior.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet access`; `cargo test --manifest-path rust/Cargo.toml --quiet`; `cargo clippy --manifest-path rust/Cargo.toml --all-targets -- -D warnings`; `make quality-architecture`; `git diff --check`.
- Impact: `rust/src/commands/access/access_runtime_org_rust_tests.rs`, `rust/src/commands/access/access_runtime_user_rust_tests.rs`, and AI trace docs. README files and Python implementation were intentionally left unchanged.
- Rollback/Risk: low test-only split. Rollback would move the user runtime tests back into the org runtime test module.
- Follow-up: continue remaining architecture-warning hotspots: dashboard browse support, dashboard dependency contract, datasource staged reading, datasource CLI mutation/tail tests, snapshot tests, sync live apply, and help-test semantic assertions.

## 2026-04-15 - Continue Rust TODO worker pass
- Summary: continued the Rust-first maintainability TODO pass with five disjoint worker slices. Datasource tail fixture builders moved into a shared test fixture module, access team command tests moved out of the main access test hub, dashboard interactive import workflow tests moved out of the browse workflow hub, datasource staged project-status reading moved behind a thin wrapper, and sync live apply result-envelope assembly moved into a focused helper module.
- Tests: preserved datasource tail/diff behavior, access team command behavior, dashboard browse interactive import behavior, datasource staged project-status output behavior, and sync live-apply response envelope behavior.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet access`; `cargo test --manifest-path rust/Cargo.toml --quiet datasource_`; `cargo test --manifest-path rust/Cargo.toml --quiet dashboard_browse`; `cargo test --manifest-path rust/Cargo.toml --quiet project_status`; `cargo test --manifest-path rust/Cargo.toml --quiet sync_live`; `cargo test --manifest-path rust/Cargo.toml --quiet`; `cargo clippy --manifest-path rust/Cargo.toml --all-targets -- -D warnings`; `make quality-architecture`; `git diff --check`.
- Impact: `rust/src/commands/datasource/tests/tail.rs`, `rust/src/commands/datasource/tests/tail_diff.rs`, `rust/src/commands/datasource/tests/tail_fixtures.rs`, `rust/src/commands/access/rust_tests.rs`, `rust/src/commands/access/access_team_rust_tests.rs`, `rust/src/commands/dashboard/dashboard_browse_workflow_rust_tests.rs`, `rust/src/commands/dashboard/dashboard_browse_workflow_interactive_import_rust_tests.rs`, `rust/src/commands/datasource/project_status/staged.rs`, `rust/src/commands/datasource/project_status/staged_reading.rs`, `rust/src/grafana/api/sync_live.rs`, `rust/src/grafana/api/sync_live_apply.rs`, `rust/src/grafana/api/sync_live_apply_result.rs`, and AI trace docs. README files and Python implementation were intentionally left unchanged.
- Rollback/Risk: low to medium maintainability refactor. Most changes are test-only splits; production changes keep wrappers around moved staged-status and live-apply envelope logic. Rollback would collapse the focused modules back into their parent files.
- Follow-up: architecture warnings remain for access runtime org tests, dashboard browse support, dashboard dependency contract, datasource staged reading, datasource CLI mutation/tail tests, snapshot tests, sync live apply, and two help-test assertion hubs.

## 2026-04-15 - Start Rust TODO maintainability pass
- Summary: implemented the first conservative Rust TODO slices through disjoint worker scopes. Datasource tail diff parser/live comparison tests moved out of the remaining tail test hub, access team browse action routing moved into a focused action module, and live status now uses an internal `ProjectDomainStatusReading` shape before rendering project-domain status output.
- Tests: preserved datasource diff behavior, team browse action behavior, and live status fallback/aggregation behavior with focused Rust regressions plus full Rust integration validation.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet datasource_`; `cargo test --manifest-path rust/Cargo.toml --quiet team_browse`; `cargo test --manifest-path rust/Cargo.toml --quiet status`; `cargo test --manifest-path rust/Cargo.toml --quiet project_status`; `cargo test --manifest-path rust/Cargo.toml --quiet`; `cargo clippy --manifest-path rust/Cargo.toml --all-targets -- -D warnings`; `make quality-architecture`; `make quality-ai-workflow`; `git diff --check`.
- Impact: `rust/src/commands/datasource/tests/tail.rs`, `rust/src/commands/datasource/tests/tail_diff.rs`, `rust/src/commands/access/team_browse.rs`, `rust/src/commands/access/team_browse_actions.rs`, `rust/src/commands/access/team_browse_input.rs`, `rust/src/commands/status/contract.rs`, `rust/src/commands/status/live.rs`, and AI trace docs. README files and Python implementation were intentionally left unchanged.
- Rollback/Risk: low to medium behavior-preserving maintainability change. Rollback would collapse the focused test/action modules and remove the internal status reading shape. The remaining risk is accidental TUI action routing drift, covered by focused team browse tests and full Rust tests.
- Follow-up: continue TODO slices for the next architecture-warning hotspots, especially the still-large datasource tail tests, access runtime tests, dashboard browse workflow tests, dashboard dependency contract, datasource staged project status, snapshot tests, and sync live apply.

## 2026-04-15 - Improve Rust maintainability priorities
- Summary: formalized the supported Rust feature matrix with a repo-owned checker and maintainer docs that keep default and `--features browser` as the maintained surfaces while documenting `--no-default-features` as unsupported. Split dashboard inspect input resolution into a focused helper module, renamed the shared dashboard review source resolver from analysis terminology to a typed `review_source` model, moved topology/impact rendering to a focused module, split datasource render/parser/payload tests out of the large datasource test hub, and extended output contract validation with nested `requiredPaths` plus `pathTypes` for dashboard review fixtures.
- Tests: updated Rust unit coverage for dashboard review source classification, dashboard inspect input resolution, datasource split test suites, dashboard policy docs/help behavior, feature matrix policy, architecture guardrails, and output contract fixtures.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet resolve_dashboard_review_source`; `cargo test --manifest-path rust/Cargo.toml --quiet resolve_dashboard_review_artifacts`; `cargo test --manifest-path rust/Cargo.toml --quiet resolve_inspect_export_import_dir`; `cargo test --manifest-path rust/Cargo.toml --quiet datasource_`; `cargo test --manifest-path rust/Cargo.toml --quiet topology`; `cargo test --manifest-path rust/Cargo.toml --quiet`; `cargo clippy --manifest-path rust/Cargo.toml --all-targets -- -D warnings`; `make quality-rust-feature-matrix`; `make quality-output-contracts`; `make quality-architecture`; `make man-check`; `make html-check`; `make quality-docs-surface`; `make quality-ai-workflow`; nested output-contract negative probe; `git diff --check`.
- Impact: `Makefile`, `scripts/check_rust_feature_matrix.py`, `scripts/output_contracts.py`, `scripts/contracts/output-contracts.json`, dashboard review/source/topology/policy Rust modules and docs, datasource Rust test modules, maintainer contract/feature-matrix docs, generated dashboard policy docs, and AI trace docs. README files were intentionally left unchanged.
- Rollback/Risk: medium internal-maintainability change. Runtime command names remain stable, but rollback would restore the old dashboard analysis-source module name, collapse the test/render/input splits, remove the feature-matrix check, and reduce output contract validation back to root-field/path-presence checks.
- Follow-up: the feature-matrix policy intentionally documents `--no-default-features` as unsupported; revisit only if the TUI code/tests are fully gated behind a clean feature boundary.
